<%= render "shared/banner2" %>

<% if @videos.any? %>
  <div class="container">
    <h1>Latest videos</h1>
    <div id="player-wrapper"></div>
    <% @videos.each do |group| %>
      <div class="row">
        <% group.each do |video| %>

            <div class="col-md-4">
              <div class="yt_video thumbnail">
                <%= link_to image_tag(video.thumbnail_url, alt: video.title,
                                      class: 'img-rounded'),
                            "https://www.youtube.com/watch?v=#{video.uid}", target: '_blank' %>
                <div class="caption">
                  <h5><%= video.title %></h5>

                  <p>
                    <span class="glyphicon glyphicon glyphicon-thumbs-up"></span> <%= video.likes %>
                    <span class="glyphicon glyphicon glyphicon-thumbs-down"></span> <%= video.dislikes %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>

      </div>
    <% end %>
  </div>
<% end %>
