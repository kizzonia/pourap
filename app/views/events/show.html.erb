<div class="technology">
	<div class="container">
		<div class="col-md-9 technology-left">
			<div class="agileinfo">

		  <h2 class="w3"><%= @event.title %>GE</h2>
			<div class="single">
        <%= image_tag @event.thumbnail.url(:medium), class: "img-responsive " %>

			    <div class="b-bottom">
			      <h5 class="top">  <%= raw(@event.shortDescription) %>
              </h5>
				   <p class="sub">  <%= raw(@event.description) %>.</p>
           <p class="sub">   <%= @event.location %> </p>
           <p class="sub">  <%= @event.eventDate %> </p>
           <p class="sub">   <%= raw(@event.sponsors) %> </p>
           <p class="sub"> Social handler @<%= @event.socialLink %> </p?


				</div>
			 </div>
     </div>
   </div>
 </div>
