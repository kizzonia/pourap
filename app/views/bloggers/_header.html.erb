<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
		<div class="container-fluid">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#sidebar-collapse">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button>
        <%= link_to "PouRap Admin", root_path, class: "navbar-brand"  %> |
				<%= "hidden" unless logged_in? %>
				  <%= link_to "Check out your profile infos", you_path, class: "navbar-brand" %>
					<% end %>


				<ul class="user-menu">
					<li class="dropdown pull-right">
						<% if logged_in? %>
						<a href="#" class="dropdown-toggle" data-toggle="dropdown"><svg class="glyph stroked male-user">
							<use xlink:href="#stroked-male-user"></use></svg> <%= current_user.soundcloud_username %>  <span class="caret"></span></a>

						<% else %>
						<a href="#" class="dropdown-toggle" data-toggle="dropdown"><svg class="glyph stroked male-user">
							<use xlink:href="#stroked-male-user"></use></svg> Blogger <span class="caret"></span></a>
							<% end %>
						<ul class="dropdown-menu" role="menu">
							<li><a href="#"><svg class="glyph stroked male-user"><use xlink:href="#stroked-male-user"></use></svg> Profile</a></li>
              <li><%= link_to  destroy_blogger_session_path, :method => :delete do  %>
                    <svg class="glyph stroked cancel"><use xlink:href="#stroked-cancel"></use></svg>
                    Logout
                    <% end %>
                  </li>


						</ul>
					</li>
				</ul>
			</div>

		</div><!-- /.container-fluid -->
	</nav>
